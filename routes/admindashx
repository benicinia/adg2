var express = require('express');
var router = express.Router();

/* GET users listing. */
router.post('/', function(req, res) {
    res.render('dasha', { title: 'Signup' });
});
router.get('/', function(req, res) {
    res.render('dasha', { title: 'Signup' });
});

module.exports = router;